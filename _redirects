# Hostname redirects are configured in Cloudflare account Bulk Redirects.
# List: every_read_canonical; rule: Every Read canonical website.
# Pages _redirects supports path rules, not hostname-level redirects.
